/**
 * PXUI Canonical Geometry Tokens
 * Established in Batch D.01 (Actions & Controls 001–010).
 * Reusable measurements governing pixel-native silhouettes across the catalog.
 */

/** Canonical authoring grid dimension (24x24) */
export const PX_GRID = 24;

/** Standard thickness for primary action crossbars and rule bars (4 grid units) */
export const ACTION_BAR_WEIGHT = 4;

/** Canonical footprint extent for primary standalone action glyphs (16x16) */
export const ACTION_STANDARD_EXTENT = 16;

/** Container boundary step chamfer for modular containers (2 grid units) */
export const CONTAINER_CORNER_STEP = 2;

/** Bar weight for secondary modifiers and compound container glyphs (2 grid units) */
export const MODIFIER_WEIGHT = 2;

/** Safe visual bounding box margin from canvas edge */
export const CANVAS_SAFE_MARGIN = 2;
